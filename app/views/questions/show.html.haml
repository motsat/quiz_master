- if flash[:answer_result]
  = flash[:answer_result]
%h3
  Q#{@question.display_order}
#{@question.title.html_safe}
= form_with url: question_answers_path(@question), local: true do |form|
  = text_area :questions, :answer
  = form.submit
